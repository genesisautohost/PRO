# Drop your video clips here, e.g. disassemble.mp4 / seam.mp4 / disclose.mp4
# Then set the matching src="/media/<file>.mp4" on each <VideoSection> in src/App.jsx.
# Recommended: H.264 MP4, 1920x1080 or 1280x720, muted, short seamless loop, < ~6 MB each.
